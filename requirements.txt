# Tokenizer Visualizer — Dependencies
streamlit>=1.35          # Web UI framework
transformers>=4.40       # HF tokenizers: BERT, GPT-2, T5, LLaMA, Phi-2
torch>=2.0               # Tensor backend for HF tokenizers
tiktoken>=0.7            # GPT-4o tokenizer (OpenAI)
sentencepiece>=0.1.99    # SentencePiece support for LLaMA / T5
protobuf>=3.20           # Required by sentencepiece